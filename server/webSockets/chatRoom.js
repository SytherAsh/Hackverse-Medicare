const { Server } = require('socket.io');
const mongoose = require('mongoose');
const { Message, Room, initializePredefinedRooms } = require('../models/Chat');
const User = require('../models/User');

const chatRoom = (io)=>{

}


module.exports = chatRoom;